SELECT DEPARTMENT_NAME, LOWER(DEPARTMENT_NAME) FROM DEPARTMENTS; 
-- LOWER, STRING DEGERININ TAMAMINI KUCUK HARFE CEVIRIR
SELECT DEPARTMENT_NAME, UPPER(DEPARTMENT_NAME) FROM DEPARTMENTS; 
-- UPPER, STRING DEGERININ TAMAMINI BUYUK HARFE CEVIRIR
SELECT DEPARTMENT_NAME, INITCAP(DEPARTMENT_NAME) FROM DEPARTMENTS;
-- INITCAP, STRING DEGERININ BAS HARFINI BUYUK HARFE CEVIRIR
SELECT DEPARTMENT_NAME, SUBSTR(DEPARTMENT_NAME,1,3) FROM DEPARTMENTS;
-- SUBSTR, STRING DEGERININ SOLDAN (1) BASLAYARAK ILK 3 HANESINI YAZAR
SELECT DEPARTMENT_NAME, LENGTH(DEPARTMENT_NAME) FROM DEPARTMENTS;
-- LENGTH, STRING DEGERININ KAC HARFTEN OLUSTUGUNU YAZAR
SELECT INSTR('KADIR CAPUR','A') FROM DUAL;
-- KADIR CAPUR STRING DEGERI ICERISINDEKI A HARFININ SOLDAN KACINCI SIRADA OLD YAZAR
SELECT REPLACE('KADIR CAPUR','KADIR','K.') FROM DUAL;
-- KADIR CAPUR METNI ICINDE KADIR KELIMESI GECIYORSA ONU K. OLARAK DEGISTIRIR
SELECT FIRST_NAME, JOB_ID REPLACE(JOB_ID,'IT','BT') FROM EMPLOYEES;
--JOB_ID KOLONUNDAKI IT YAZAN YERLERI BT OLARAK DEGISTIRIR
SELECT TRANSLATE('ORACLE SQL','ROAS','1234') FROM DUAL;
-- R HARFI 1, O HARFI 2, A HARFI 3, S HARFI 4 OLUR
SELECT TRANSLATE('MERHABA KADIR HOCAM' ORJ_MESAJ,'ABCDEFGHIJKLMNOPQRSTUVWXYZ abcdefghijklmnopqrstuvwxyz',
                                        'BCDEFGHIJKLMNOPQRSTUVWXYZA bcdefghijklmnopqrstuvwxyza')
                                        SIFRELI_MESAJ FROM DUAL;
                                        
SELECT TRANSLATE('MERHABA KADIR HOCAM' ORJ_MESAJ,'ABCDEFGHIJKLMNOPQRSTUVWXYZ abcdefghijklmnopqrstuvwxyz',
                                        'BCDEFGHIJKLMNOPQRSTUVWXYZA bcdefghijklmnopqrstuvwxyza')
                                        SIFRELI_MESAJ FROM DUAL;
                                        
SELECT * FROM EMPLOYEES;                                        

SELECT LAST_NAME, RPAD(LAST_NAME,20,*') FROM EMPLOYEES; 
-- LAST_NAME DIYELIM KI CAPUR, CIKTIYA CAPUR*************** YAZAR
SELECT LAST_NAME, LPAD(LAST_NAME,20,*') FROM EMPLOYEES;

SELECT LTRIM('     ORACLE SQL   '), RTRIM('   ORACLE SQL    ') FROM DUAL;
--ORACLE SQL      ,       ORACLE SQL,
SELECT TRIM('' FROM'     ORACLE SQL   ')
--ORACLE SQL,

SELECT FIRST_NAME, LAST_NAME FROM EMPLOYEES;

SELECT CONCAT(FIRST_NAME, LAST_NAME) FROM EMPLOYEES;
-- ISIM SOYISMI BIRLESTIRIP TEK SUTUNA YAZAR, ARADA BOSLUK OLMAZ
SELECT CONCAT(CONCAT(FIRST_NAME,''),LAST_NAME) FROM EMPLOYEES;
-- ARADA BOSLUK OLUR
SELECT FIRST_NAME [''] LAST_NAME FROM EMPLOYEES;
-- USTTEKI CIKTI ILE AYNI

SELECT CONCAT(CONCAT(FIRST_NAME, 'IS A'), JOB_ID) FROM EMPLOYEES;
-- STEVEN IS A AD_PRES
SELECT FIRST NAME ['IS A'] JOB_ID FROM EMPLOYEES;
-- STEVEN IS A AD_PRES
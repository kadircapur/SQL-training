DESC EMPLOYEES_COPY;

SELECT EMPLOYEE_ID, SALARY, COMISSION_PCT FROM EMPLOYEES_COPY;

--Senaryo

-- 101 ve 102 sicil no lu calisanlarin maaslarini 400$ dusurup komisyon oranlarini sifirlayalim.
UPDATE EMPLOYEES_COPY
SALARY = SALARY - 400,
COMMISSION_PCT = NULL
WHERE EMPLOYEE_ID IN (101,102);

--100 NOLU BOLUMDE CALISANLARIN MAASINI ZAMDAN ONCEKI HALINE UPDATE EDELIM
--BUNUN ICIN BASKA BIR TABLODAKI EMPLOYEES DEGERI,
--DIGER TABLOYA EMPLOYEES_COPY UPDATE EDECEGIZ

SELECT EMPLOYEE_ID, SALARY, COMMISSION_PCT FROM EMPLOYEES_COPY
WHERE DEPARTMENT_ID = 100;

UPDATE EMPLOYEES_COPY A SET
A.SALARY = (SELECT B.SALARY FROM EMPLOYEES
WHERE A.EMPLOYEES_ID = B.EMPLOYEE_ID)
WHERE A.DEPARTMENT_ID = 100;

COMMIT;

UPDATE EMPLOYEES_COPY A SET
A.SALARY = (SELECT B.SALARY FROM EMPLOYEES
WHERE A.EMPLOYEES_ID = B.EMPLOYEE_ID)
WHERE A.DEPARTMENT_ID = 90;


DELETE FROM EMPLOYEES_COPY; --VERILERIN TAMAMINI SILER (COMMIT YAZMAK GERKEIR)

INSERT INTO EMPLOYEES_COPY
SELECT * FROM EMPLOYEES; -- SILINENLERI GERI GETIRIR (COMMIT YAZMAK GEREKIR)

DELETE FROM EMPLOYEES_COPY
WHERE DEPARTMENT_ID = 90;
COMMIT;
